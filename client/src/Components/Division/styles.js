import styled from "styled-components";

export const DivisionStyle = styled.div`
  border-bottom: 2px solid var(--components-background);
  width: 100%;
  margin: 48px 0;

  .banner {
    background: var(--light-background);
    height: 100%;
    p {
      font-size: 24px;
      text-align: center;
      /* width: 15px; */
    }
  }
`;